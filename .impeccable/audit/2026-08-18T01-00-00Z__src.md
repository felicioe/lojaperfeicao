# Audit Report — src
**Target:** `src/` · **Date:** 2026-08-18 · **Score: 17/20 — Good**

## Validation Results (pre-audit)
| Check | Result |
|---|---|
| Detector (`detect.mjs --json src/`) | ⚠ 1 warning: `overused-font` — Inter — `src/routes/__root.tsx:109` (unchanged, advisory) |
| Prior audit on file | `2026-08-18T00-00-00Z__src.md` — 15/20 |

This is a re-audit. Both P1 findings from the prior pass were fixed in this session (39 route files touched) and are re-verified against current source below, not carried forward on trust.

---

## Dimension 1 — Accessibility · 3/4 (up from 2/4)

**Fixed since last audit:**
- **The systemic label gap is closed.** Verified by direct count: `src/routes/**/*.tsx` contains 232 `<Label>` elements, 229 now carry a matching `htmlFor`/`id`. The remaining 3 are legitimate: 2 are group labels (`usuarios/index.tsx` RadioGroup, `tesouraria/faturas/index.tsx` RateioBuilder) correctly wired via `id` + `role="group" aria-labelledby`, and 1 (`painel/eventos.tsx`) is a `<Label>` already nested inside a native `<label>` wrapping its `<Checkbox>`, which inherits an accessible name by nesting.
- The shared `Field` component (`irmaos/$id.tsx`, 35 call sites) now accepts and propagates `id`→`htmlFor`, fixing all its instances plus 17 standalone `<Label>`s in the same file's panels (Corpos/Elevações/Formação/Filhos/Parentes) in one pass. A duplicate-`id` bug was caught and fixed in the process: `ParentesPanel` renders 5 times on the same page (Cônjuge, Contato de emergência, Pai, Mãe, Outros) and previously would have produced 5 identical DOM ids — field ids now incorporate `tipo` to stay unique.

**Still open (unchanged, out of this session's scope):**
- `tesouraria/contas.tsx:146-157` — icon-only expand/collapse `<Button variant="ghost">` still has no `aria-label`.
- `tesouraria/conciliacao.tsx:197,232` — OFX/system list row selection still conveyed only via a `bg-muted` class swap; no `aria-pressed`/`aria-selected`.
- No `aria-live`/`aria-busy` region for query loading states outside of Sonner toasts.

**Score 0-4**: 3=Good (WCAG AA mostly met — the dominant, cross-cutting violation is resolved; the two remaining gaps are isolated to single components, not systemic).

---

## Dimension 2 — Performance · 3/4 (unchanged)

Not in scope this session. Same findings as prior audit: no route-level code splitting (63 route files bundle together, acceptable at current scale), no virtualization on `lancamentos`/`saldos`/`conciliacao` tables.

---

## Dimension 3 — Theming · 4/4 (unchanged)

Not in scope this session. Still 100% oklch tokens, zero hardcoded colors, full dark-mode parity. Detector's only flag remains the advisory `overused-font: Inter`.

---

## Dimension 4 — Responsive Design · 3/4 (unchanged)

Not in scope this session. `tesouraria/index.tsx:189-259` still fully hides Emissão/Categoria columns on mobile rather than reflowing them; `ChavesPixPanel`'s "Adicionar" button is still intrinsic-width rather than full-width on mobile (minor).

---

## Dimension 5 — Implementation Integrity · 4/4 (up from 3/4)

**Verdict:** The implementation remains a coherent, product-specific system. Both P1s flagged in the prior audit — the systemic label gap and silent dashboard fetch failures — are now fixed and verified in code, not just claimed.

**Fixed since last audit:**
- `dashboard.tsx` — all 6 `useQuery` calls now surface `isError` with a "Tentar novamente" retry path: the 4 `MetricCard` metrics via new optional `isError`/`onRetry` props, and the "Contas a pagar" table via an `isError` branch in `TableBody`, distinct from its empty-state branch.
- `painel/index.tsx` — the blocking `meuIrmao` query now branches on `isError` (alongside the existing `isLoading` check) into an `EmptyState` with retry, matching the pattern in `tesouraria/index.tsx:231-241`. The three secondary queries on the same page (`lancamentos`, `frequencia`, `naoLidos`) were deliberately left as-is — they already degrade gracefully via `?? []`/`?? 0` and only omit optional widgets rather than blocking the page, so they don't carry the same risk the audit flagged.

**Still open (unchanged, P3, out of scope):**
- `MetricCard` remains duplicated (`painel/index.tsx:273`, `dashboard.tsx:207`) — and slightly more divergent now, since dashboard's copy gained `isError`/`onRetry` props the painel copy doesn't have.
- Root `<head>` metadata in `__root.tsx:83-102` is still English against an otherwise fully Portuguese product surface.
- Detector's advisory `overused-font: Inter` — unchanged, aesthetic only.

**Score 0-4**: 4=Coherent and intentional (no P0/P1/P2 remain in this dimension; the two residual findings are isolated P3 polish items, not drift).

---

## Score Summary

| # | Dimension | Score | Key Finding |
|---|-----------|-------|-------------|
| 1 | Accessibility | 3/4 | Systemic label gap closed (232 labels, 229 direct + 3 legitimate exceptions); 2 isolated gaps remain |
| 2 | Performance | 3/4 | Unchanged — no code splitting or table virtualization, not yet a problem at current scale |
| 3 | Theming | 4/4 | Unchanged — 100% oklch tokens, full dark-mode parity |
| 4 | Responsive Design | 3/4 | Unchanged — two table columns still fully hidden (not reflowed) on mobile |
| 5 | Implementation Integrity | 4/4 | Both P1s fixed (labels, silent dashboard errors); only P3 polish items remain |
| **Total** | | **17/20** | **Good** |

Rating bands: 18-20 Excellent, 14-17 Good, 10-13 Acceptable, 6-9 Poor, 0-5 Critical.

---

## Detailed Findings by Severity

**[P2] Icon-only control without accessible name**
- **Location:** `tesouraria/contas.tsx:146-157` (expand/collapse chevron button)
- **Category:** Accessibility
- **Recommendation:** Add `aria-label="Expandir chaves Pix"` / `"Recolher chaves Pix"` (toggle by `expandido === c.id`) plus `aria-expanded`.
- **Suggested command:** `/impeccable harden`

**[P2] Selection state conveyed by color only**
- **Location:** `tesouraria/conciliacao.tsx:197,232`
- **Category:** Accessibility
- **Recommendation:** Add `aria-pressed={selSistema === s.id}` (or `role="option"`/`aria-selected`) to the selectable buttons.
- **Suggested command:** `/impeccable clarify`

**[P2] Financial data fully hidden (not reflowed) on mobile**
- **Location:** `tesouraria/index.tsx:189-259` — Emissão and Categoria columns
- **Category:** Responsive Design
- **Recommendation:** Add an expandable row or a compact secondary line under the primary cell for narrow viewports.
- **Suggested command:** `/impeccable adapt`

**[P3] Duplicate `MetricCard` component (now more divergent)**
- **Location:** `painel/index.tsx:273`, `dashboard.tsx:207`
- **Category:** Implementation Integrity
- **Recommendation:** Extract to `@/components/app/MetricCard.tsx` with a unified prop surface (`tone`, `isError`, `onRetry`).
- **Suggested command:** `/impeccable extract`

**[P3] Root metadata language mismatch**
- **Location:** `src/routes/__root.tsx:83-102`
- **Category:** Implementation Integrity
- **Recommendation:** Localize root `<head>` metadata to Portuguese, or confirm intentional.
- **Suggested command:** `/impeccable clarify`

**[P3] No route-level code splitting**
- **Location:** All route files (`src/routes/`)
- **Category:** Performance
- **Suggested command:** `/impeccable optimize`

**[P3] Overused font: Inter**
- **Location:** `src/routes/__root.tsx:109`, `src/styles.css:65`
- **Category:** Implementation Integrity (detector, advisory)
- **Suggested command:** `/impeccable typeset`

---

## Patterns & Systemic Issues

- No systemic issues remain. The one cross-cutting gap in the app (label association) is resolved app-wide, including a shared-component fix (`Field`) that closed 35 instances at once and caught a real duplicate-id bug along the way.
- Everything still open is isolated to 1-2 files or is cosmetic/advisory.

## Positive Findings

- The label fix didn't stop at surface-level patching: it found and fixed a shared `Field` component to resolve 35 instances structurally, and caught a duplicate-DOM-id bug (`ParentesPanel` × 5) that predates this audit and wasn't in the original finding.
- Both P1s were fixed with typecheck clean across the whole session (39 files, 843 insertions / 363 deletions) and no regressions in the areas already scored 4/4 (Theming) or 3/4 (Performance, Responsive).
- Error-state handling is now consistent across all three dashboard-shaped screens (`tesouraria/index.tsx`, `dashboard.tsx`, `painel/index.tsx`) using the same retry pattern, closing the propagation gap flagged last audit.

---

## Recommended Actions

1. **[P2] `/impeccable harden`**: Add `aria-label`/`aria-expanded` to the `contas.tsx` expand button and `aria-pressed` to `conciliacao.tsx` row selection.
2. **[P2] `/impeccable adapt`**: Surface the hidden Emissão/Categoria columns on mobile in `tesouraria/index.tsx`.
3. **[P3] `/impeccable extract`**: Deduplicate `MetricCard`, now with a slightly wider API gap than before.
4. **[P3] `/impeccable clarify`**: Localize root `<head>` metadata to Portuguese (or confirm intentional).
5. **[P3] `/impeccable optimize`**: Consider route-level code splitting before the bundle becomes a felt problem.
6. **[P3] `/impeccable typeset`**: Consider a less-templated type pairing if a more distinctive brand voice is wanted.

You can ask me to run these one at a time, all at once, or in any order you prefer.

Re-run `/impeccable audit` after fixes to see your score improve.
